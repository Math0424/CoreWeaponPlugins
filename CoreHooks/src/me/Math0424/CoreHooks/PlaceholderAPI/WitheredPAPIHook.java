package me.Math0424.CoreHooks.PlaceholderAPI;

/*
public class WitheredPAPIHook extends PlaceholderExpansion {

    @Override
    public String getIdentifier() {
        return "withered";
    }

    @Override
    public String getAuthor() {
        return "Math0424";
    }

    @Override
    public String getVersion() {
        return Main.getPlugin().getDescription().getVersion();
    }


    @Override
    public String onPlaceholderRequest(Player p, String identifier){

        if(identifier.equalsIgnoreCase("squadName")){
            if (Squad.isInSquad(p)) {
                return Squad.getSquad(p.getName()).getName();
            }
        }

        if(identifier.equalsIgnoreCase("squadColor")){
            if (Squad.isInSquad(p)) {
                return Squad.getSquad(p.getName()).getColor().toString();
            }
        }

        if(identifier.equalsIgnoreCase("squadOwner")){
            if (Squad.isInSquad(p)) {
                return Squad.getSquad(p.getName()).getOwner().getName();
            }
        }

        return null;
    }

}
*/